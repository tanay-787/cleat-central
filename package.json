{
    "name": "cleat-central-root",
    "version": "0.1.0",
    "private": true,
    "scripts": {
      "install-deps": "pnpm install --recursive",
      "build": "cd frontend && pnpm run build"
    }
  }
  